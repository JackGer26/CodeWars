// Create a function named combineNames/combine_names/CombineNames that accepts two parameters (first and last name). The function should return the full name.

/*
PREP

Params: Two strings
Returns: One string
Example: With "James" as the first name and "Stevens" as the last name should return "James Stevens"
Psuedo:
// Create function with name combineNames
// Return parameters combined as one string
*/

let combineNames = (first, last) => `${first} ${last}`

